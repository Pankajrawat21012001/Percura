/**
 * Persona Segment Evaluation Engine
 * 
 * This engine handles processing of retrieved persona segments
 * and their interaction with the startup idea.
 */

const { testSegmentResonance } = require('./segmentTest.js');

module.exports = {
    testSegmentResonance,
};
